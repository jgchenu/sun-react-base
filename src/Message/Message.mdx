---
name: Message
route: /message
---

import "../../styles/index.less";
import Message from "./Message";
import Button from "../Button";
import { Playground, Props } from "docz";

# Messages

<Props of={Message} />

## Info Message

<Playground>
  <Button
    onClick={() => {
      Message.info({ title: " Info message" });
    }}
  >
    Info message
  </Button>
</Playground>

## Warning Message

<Playground>
  <Button onClick={() => Message.warning({ title: "Warning message" })}>
    Warning message
  </Button>
</Playground>

## Success Message

<Playground>
  <Button onClick={() => Message.success({ title: "Success message" })}>
    Success message
  </Button>
</Playground>

## Error Message

<Playground>
  <Button
    onClick={() => Message.error({ title: "Error message" })}
    btnType="danger"
  >
    Error message
  </Button>
</Playground>

## Loading Message

<Playground>
  <Button
    onClick={() => Message.loading({ title: "Loading message" })}
    btnType="primary"
  >
    Loading message
  </Button>
</Playground>

## Dark Message

<Playground>
  <Button
    onClick={() => Message.info({ title: "Dark Theme message", theme: "dark" })}
    btnType="primary"
  >
    Dark Theme message
  </Button>
</Playground>
